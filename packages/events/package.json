{
  "name": "@avkash/events",
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Foundation: transactional outbox event bus. publish() writes in the caller's tx; a relay fans out to in-process subscribers.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "@avkash/shared": "workspace:*",
    "@avkash/db": "workspace:*",
    "zod": "^4.0.0",
    "drizzle-orm": "^0.38.0"
  },
  "devDependencies": {
    "@avkash/tsconfig": "workspace:*",
    "@avkash/eslint-config": "workspace:*",
    "@types/bun": "^1.1.14",
    "typescript": "^5.8.2"
  }
}
