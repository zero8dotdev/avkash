{
  "name": "@avkash/config",
  "version": "0.0.0",
  "private": true,
  "description": "Foundation: Zod-validated environment. Fail fast at boot, not at request time.",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "@avkash/shared": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@avkash/tsconfig": "workspace:*",
    "@avkash/eslint-config": "workspace:*",
    "typescript": "^5.8.2"
  }
}
