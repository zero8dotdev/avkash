{
  "name": "@avkash/app",
  "version": "0.0.0",
  "private": true,
  "description": "Platform: createApp factory — mounts middleware, health, auth, and module routes. Both apps/api and apps/cloud-api use this.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "@avkash/shared": "workspace:*",
    "@avkash/i18n": "workspace:*",
    "@avkash/auth": "workspace:*",
    "@avkash/authz": "workspace:*",
    "@avkash/db": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@avkash/tsconfig": "workspace:*",
    "@avkash/eslint-config": "workspace:*",
    "@types/bun": "^1.1.0",
    "typescript": "^5.8.2"
  }
}
